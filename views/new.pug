extends layout

block content
  form(method="post", action="/schedules").my-3
    input(type="hidden" name="_csrf" value!=csrfToken)
    div.form-group
      label(for="scheduleName") 予定名
      input(type="text" name="scheduleName")#scheduleName.form-control
    div.form-group
      label(for="memo") メモ
      textarea(name="memo")#memo.form-control
    div.form-group
      label(for="candidates") 候補日程
      div
        input(type="date" name="candidates")#candidates.form-control
        button(type="button" id="addCandBtn" name="addCandidate").btn.btn-info.btn-sm.mt-2 候補日程を増やす( 3 つまで)
    button(type="submit" id="makeSchedBtn" disabled).btn.btn-primary 予定をつくる
